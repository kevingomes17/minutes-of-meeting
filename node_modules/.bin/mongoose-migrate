#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mongoose-migrate/bin/migrate" "$@"
  ret=$?
else 
  node  "$basedir/../mongoose-migrate/bin/migrate" "$@"
  ret=$?
fi
exit $ret
